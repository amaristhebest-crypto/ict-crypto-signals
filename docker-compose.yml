version: '3.8'

services:
  ict-signals:
    build: .
    container_name: ict-crypto-signals
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    command: ["python", "main.py", "--mode", "run"]
